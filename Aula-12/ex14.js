const palavra = "maria de fátima";
const palavras = nome.split(" ");
const novasPalavras = palavras.map(p1 => {
    return pl[0].toUpperCase() + pl.slice(1);
});
const novoNome = novasPalavras.join (" ");
console.log(nome);
console.log(novoNome);